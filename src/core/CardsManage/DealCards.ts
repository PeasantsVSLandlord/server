/**
 * 发牌
 * @param player 玩家人数，默认3人
 * @param num 发牌套数，默认1套
 * @param isShuffle 是否洗牌，默认洗
 */
function dealCards(player: number = 3, num: number = 1, isShuffle: boolean = true) {
  console.log(num, isShuffle)
}

