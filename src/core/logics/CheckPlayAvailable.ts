import { Card } from "../CardsDeclare";


/**
 *
 * @param cards
 * @param lastCards
 */
export function checkPlayAvailable(cards: Array<Card>, lastCards?: Array<Card>): boolean {
    if (lastCards) {

    } else return true;
}
